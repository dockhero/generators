Remember to update your Heroku config variables before running the stack:
  heroku config:set RETHINKDB_PASSWORD=<db connection password>
  heroku config:set RETHINKDB_ADMIN_PASSWORD=<admin UI password>
